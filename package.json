{
  "name": "obsidian-web-clipper",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun build ./src/clipper.ts --outdir=./dist --minify --target=browser --format=iife && bun run generate-bookmarklet.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@tehshrike/readability": "^0.2.0",
    "turndown": "^7.2.2"
  }
}
