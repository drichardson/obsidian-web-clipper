{
  "name": "obsidian-web-clipper",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun build ./src/clipper.ts --outdir=./dist --minify --target=browser --format=iife && bun run generate-bookmarklet.ts && bun run validate",
    "validate": "html-validate dist/index.html"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "html-validate": "^10.2.1"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@tehshrike/readability": "^0.2.0",
    "turndown": "^7.2.2"
  }
}
